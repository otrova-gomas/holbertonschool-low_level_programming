
#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9.
 * 
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int nu;

	for (nu = 48; nu < 58; nu++)
	{
		putchar(nu);
	}
	putchar('\n');
	return (0);
}
