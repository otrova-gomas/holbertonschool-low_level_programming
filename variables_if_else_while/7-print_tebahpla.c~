
#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char zA;

	for (zA = 'z'; zA >= 'a'; zA--)
		putchar(zA);
	
	putchar('\n');
	return (0);
}
