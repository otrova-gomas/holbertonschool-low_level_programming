#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - Prints the alphabet without q and e.
 *
 *
 * Return: Always 0
 */
int main(void)
{
  Char aa;

 for(aa = 'a'; aa <= 'z'; aa++)
   if (aa != 'e' && la != 'q')
   putchar(aa);


	putchar('\n');
	
	return (0);
}
