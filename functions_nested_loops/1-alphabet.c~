#include <stdio.h>

/**
 *main - print the alphabet in lowercase
 *
 *Always Return: 0
 *
 */
void print_alphabet(void)
  
{
char az;

for(az = 'a'; az <= 'z'; az++)
{
putchar (az);
}
 
putchar ('\n');
}
