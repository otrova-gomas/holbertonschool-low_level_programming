#include <stdio.h>

/**
 * jack_bauer - Prints every minute of the day
 */
void jack_bauer(void)
{
int hr, min;
for (hr = 0; hr < 24; hr++)
{
for (min = 0; min < 60; min++)
{ printf("%02d:%02d\n", hr, min);} }
